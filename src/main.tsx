import React from 'react';
import ReactDOM from 'react-dom/client';
import CaCCharacterSheet from './CaCCharacterSheet';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CaCCharacterSheet />
  </React.StrictMode>
);
